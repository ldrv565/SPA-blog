import * as React from 'react';

import Text from '../Text/Text';

const Meta = props => (
    <Text modifier="meta">
        {`MARCH ${props.id}, 2016 | TRAVEL`}
    </Text>
);

export default Meta;
